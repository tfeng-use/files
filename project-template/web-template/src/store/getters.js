// 根级别getters
export const userInfo = (state) => {
  return state.userInfo
}
export const getBreadcrumb = (state) => {
  return state.breadcrumb
}
export const currentCrumb = (state) => {
  return state.currentCrumb
}
export const slideComponent = (state) => {
  return state.slideComponent
}
export const currentMenu = (state) => {
  return state.currentMenu
}
export const leftMenu = (state) => {
  return state.leftMenu
}
export const isOpenSale = (state) => {
  return state.isOpenSale
}
